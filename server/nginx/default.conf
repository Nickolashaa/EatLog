server {
    listen 80;
    server_name api.eat-log.ru;

    location = /healthz {
        access_log off;
        return 200 "ok\n";
    }

    location / {
        proxy_pass http://api:5285;
    }
}
