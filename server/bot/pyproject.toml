[project]
name = "bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiogram>=3.28.2",
    "aiohttp>=3.13.5",
    "pydantic>=2.13.4",
]

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.mypy]
strict = true

[dependency-groups]
dev = [
    "mypy>=2.1.0",
    "ruff>=0.15.16",
]
