query GetUserByTelegramId($telegramId: String!){
  userByTelegramId(
    telegramId: $telegramId,
  )
  {
    ... on User {
      id
      name
      telegramId
    }
    ... on ObjectNotFoundError {
      message
    }
  }
}
