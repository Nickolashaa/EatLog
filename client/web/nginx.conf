server {
    listen 80;
    listen [::]:80;

    root /usr/web/;
    index index.html;

    location /health {
        access_log off;
        return 200 "ok\n";
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
